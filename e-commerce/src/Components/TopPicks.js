import React from "react";
import data from "../data";
import ProductCard from "./ProductCard";

const TopPicks = () => {
  return (
    <section>
      {/* data.getProductList.map((product) => (<ProductCard name={product.name} /> */}
      ))
    </section>
  );
};

export default TopPicks;
